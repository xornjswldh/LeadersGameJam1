

function on_message(self, message_id, message, sender)
	if message_id==hash("spawn") then
		factory.create("#character",go.get_position("/party"..tostring(message.num)),nil,{anim=message.anim},4)
	end
end
